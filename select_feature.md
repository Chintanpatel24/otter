# Select Any Feature to Implement

Choose one (or more) by number or name:

1. Quantized streaming (Colibri-style expert stream from disk, not full RAM load)
2. Arena comparison (diff/highlight view for model answers side-by-side)
3. Real-time stream panel (live terminal showing token generation speed)
4. Per-model config (temperature, max tokens, threads per model in config.json)
5. WebSocket API stream (/v1/stream with token chunks)
6. Benchmark script (CPU vs CUDA speed test, tokens/sec logging)
7. Docker server (Dockerfile for independent API deployment)
8. Export results (.md or .json output with model names and timestamps)
9. Drag URL support (paste Hugging Face URLs into drag zone)
10. Mesh TCP sockets (live peer load balancing over sockets, not file registry)
