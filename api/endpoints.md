# Otter API Endpoints

- `GET /v1/health` - Health check
- `GET /v1/models` - List available .gguf models
- `POST /v1/chat/completions` - Chat with tool support (OpenAI-compatible)
- `POST /v1/completions` - Text completion with token rate tracking
